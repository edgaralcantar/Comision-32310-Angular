import { FiltroCursosCursosPipe } from './filtro-cursos-cursos.pipe';

describe('FiltroCursosCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCursosCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
