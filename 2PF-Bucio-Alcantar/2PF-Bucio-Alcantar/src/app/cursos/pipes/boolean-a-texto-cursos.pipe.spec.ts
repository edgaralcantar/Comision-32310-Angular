import { BooleanATextoCursosPipe } from './boolean-a-texto-cursos.pipe';

describe('BooleanATextoCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanATextoCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
