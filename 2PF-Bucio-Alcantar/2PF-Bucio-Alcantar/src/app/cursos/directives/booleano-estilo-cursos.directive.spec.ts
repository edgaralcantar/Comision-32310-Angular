import { BooleanoEstiloCursosDirective } from './booleano-estilo-cursos.directive';

describe('BooleanoEstiloCursosDirective', () => {
  /*it('should create an instance', () => {
    let directive = new BooleanoEstiloCursosDirective();
    expect(directive).toBeTruthy();
  });*/
});
