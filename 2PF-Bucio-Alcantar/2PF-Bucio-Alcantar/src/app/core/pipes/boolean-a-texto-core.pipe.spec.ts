import { BooleanATextoCorePipe } from './boolean-a-texto-core.pipe';

describe('BooleanATextoCorePipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanATextoCorePipe();
    expect(pipe).toBeTruthy();
  });
});
