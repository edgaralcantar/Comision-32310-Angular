import { FiltroCursosCorePipe } from './filtro-cursos-core.pipe';

describe('FiltroCursosCorePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCursosCorePipe();
    expect(pipe).toBeTruthy();
  });
});
