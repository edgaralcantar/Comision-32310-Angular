import { BooleanoEstiloCoreDirective } from './booleano-estilo-core.directive';

describe('BooleanoEstiloCoreDirective', () => {
  /*it('should create an instance', () => {
    const directive = new BooleanoEstiloCoreDirective();
    expect(directive).toBeTruthy();
  });*/
});
 