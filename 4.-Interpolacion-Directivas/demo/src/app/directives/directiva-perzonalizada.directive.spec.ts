import { DirectivaPerzonalizadaDirective } from './directiva-perzonalizada.directive';

describe('DirectivaPerzonalizadaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPerzonalizadaDirective();
    expect(directive).toBeTruthy();
  });
});
